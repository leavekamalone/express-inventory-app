extends layout

block content
  h1= title
  p Welcome to #[em InventoryApplication], an Inventory Application for video games

  h1 Inventory content

  if error
    p Error getting dynamic content.
  else
    p The inventory has the following counts:

    ul
      li #[strong Games:] !{data.game_count}
      li #[strong Categories:] !{data.category_count}